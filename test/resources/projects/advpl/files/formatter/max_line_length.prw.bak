//Não enviar ao repositório se aplicar formatação
#include "protheus.ch"

user function maxLineLength()
	local n := 1
	conout("value one", "value two", "value three", "value four", "value five", "value six", "value seven")

conout("Para validação de encoding: não, é, ção")

	return
