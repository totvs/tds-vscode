//Não enviar ao repositório se aplicar formatação
#include "protheus.ch"

user function f1()
        if .t.
    conout("a")
	   return
endif

conout("Para validação de encoding: não, é, ção")

  return

user function f2()
        if .t.
    conout("a")
	   return
endif
  return
