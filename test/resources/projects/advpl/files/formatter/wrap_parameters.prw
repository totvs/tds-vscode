//Não enviar ao repositório se aplicar formatação
#include "protheus.ch"

user function onlyOne(pOne)
local OnlyOne := .t.
return

user function wrapParams(pOne, pTwo, pThree)
return

static function helper(pAlpha, pBeta)
return

method myMethod(pX, pY, pZ) class MyClass
return
