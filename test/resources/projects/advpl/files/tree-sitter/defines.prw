#include "totvs.ch"

#define MY_CONST 100
#define MODE
#define PSEUDO_FUN(X) (x*10)

user function directives(p1)
  local nVar := MY_CONST

  conout(MY_CONST)
  conout(MODE)
  conout(PSEUDO_FUNC(10))


return

