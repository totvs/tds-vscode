{
  "name": "tds-vscode-shared",
  "version": "0.0.1",
  "description": "TDS-VSCode shared library",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/totvs/tds-vscode.git"
  },
  "bugs": {
    "url": "https://github.com/totvs/tds-vscode.git"
  },
  "main": "./dist/index.js",
  "scripts": {
    "compile": "tsc --build",
    "build": "tsc --build",
    "watch": "tsc --watch",
    "prepublishOnly": "tsc --build --clean && pnpm run build"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "dependencies": {
    "@types/vscode": "^1.85.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0"
  }
}